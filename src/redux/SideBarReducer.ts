import { RootStateType} from './state';
import {DialogsReducerAcTypes} from './DialogsReducer';

const DialogsReducer = (state: RootStateType, action: DialogsReducerAcTypes): RootStateType => {

    return state
}

export default DialogsReducer;
